>rubricacl.php
This file is copied automatically if apache user has permission to write to the mahara's docroot,
otherwise you must copy it to the directory below.
${docroot}lib/form/elements

>rubricacl.tpl
This file is copied automatically if apache user has permission to write to the mahara's docroot,
otherwise you must copy it to the directory below.
${docroot}theme/raw/templates/form